//chose a digital pin to read the ir sensor to//then connect the power and ground //the ir sensor does need to be adjusted using the potentiometer./*put your hand in front of the sensor at the distance that you wish the sensor to trigger,then turn the potentiometer until you find the spot where you can turn it on and offthen set it where you hand trigger where you want it to */int irsensor=4;bool ir();//runs on startupvoid setup() {        //set irsensor to input    pinMode(irsensor,INPUT);        //start the serial monitor at 9600 bauds    Serial.begin(9600);}//runs forever while arduino is onvoid loop() {  //if the ir sensor detects soemthing then it will print to the serial monitor  if(ir()){    Serial.println("Object Detected");  }   }//returns true if object is detected and vise versabool ir(){  return digitalRead(irsensor);}